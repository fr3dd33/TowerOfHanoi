;--------------------------------------------------------------------
;
; Tower of Hanoi is a game initially written in C++. 
; Later, ported to ASM in purpose of practise.
;
;--------------------------------------------------------------------

;--------------------------------------------------------------------
;
; ASM version written in MASM syntax and works in text mode 
; only with BIOS interrupts. In order to compile, download
; MASM compiler from masm32.com. No need to compile all files 
; just MAIN.ASM file using compile options:
;       ml.exe /c MAIN.ASM
;       link16.exe /T MAIN.OBJ
;
;--------------------------------------------------------------------